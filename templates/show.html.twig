{% extends 'base.html.twig' %}

{% block body %}
	{# <div class="max-w-2xl flex flex-col items-center mx-auto py-10 px-6 bg-white shadow-lg rounded-lg">
		<h1 class="text-3xl font-bold text-purple-700 mb-4">{{ post.title }}</h1>
	
		<p class="text-gray-600 mb-2">
			<strong class="text-gray-800">Author:</strong> {{ post.author }}
		</p>
	
		<p class="text-lg text-gray-800 mb-6">{{ post.content }}</p>
	
		<h2 class="text-2xl font-semibold text-purple-600 mb-4">Comments:</h2>
		<ul class="space-y-4">
			{% for comment in post.comments %}
				<li class="border border-gray-200 p-4 rounded-md bg-gray-50">
					<p class="text-sm text-gray-600">
						<strong class="text-gray-800">{{ comment.author }}:</strong> {{ comment.content }}
					</p>
				</li>
			{% else %}
				<li class="text-gray-500">No comments yet.</li>
			{% endfor %}
		</ul>
	    <a href="{{'/blog'}}" class="bg-purple-600  text-white px-4 py-2 rounded-full mt-6 hover:text-purple-300 cursor-pointer transition-all">Back to all posts</a>
	</div> #}
	<div class="mt-10 flex justify-center items-center text-2xl font-bold">
	<h1>Post Details and Comments</h1>
	</div>
  
   <div class="flex flex-col items-center mt-10 gap-8">
	{{form_start(form)}}
	<div class="bg-red-500">
		{{form_errors(form)}}
	</div>
	<div class="mb-6">
		{{form_widget(form.author)}}
		{{form_errors(form.author)}}
	</div>
	<div class="mb-6">
		{{form_widget(form.content)}}
		{{form_errors(form.content)}}
	</div>
	<button class="cursor-pointer text-lg bg-purple-500 text-white hover:text-purple-400 px-4 py-2 rounded-2xl" type="submit">Create Comment</button>
	{{form_end(form)}}
   </div>
{% endblock %}
